<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="detector.wsn.ustc.com.detectorservice">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    -
    <application
        android:name="com.ustc.wsn.detector.Application.AppResourceApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:persistent="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.ustc.wsn.detector.activity.MainActivity"
            android:label="@string/app_name">
            <intent-filter android:priority="1000">
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name="com.ustc.wsn.detector.activity.DetectorActivity"/>
        <service
            android:name="com.ustc.wsn.detector.service.DetectorService"
            android:enabled="true">
            <intent-filter android:priority="1000">
                <action android:name="com.ustc.wsn.detector.activity.DetectorActivity" />
            </intent-filter>

        </service>

        <service
            android:name="com.ustc.wsn.detector.service.AutoUploadSeriver">
            <intent-filter android:priority="1000">
                <action android:name="com.ustc.wsn.detector.service.AutoUploadSeriver" />
            </intent-filter>
        </service>

    </application>

</manifest>